[package]
name = "ccf-basic-rust"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
ccf-rs = { path = "../../../src/rust" }

[profile.release]
lto = true
codegen-units = 1
