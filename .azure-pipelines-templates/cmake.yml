parameters:
  cmake_args: ""

steps:
  - template: run_on_target.yml
    parameters:
      commands: |
        set -ex
        ls -la
        mkdir build
        cd build
        cmake -L -GNinja .. ${{ parameters.cmake_args }}
      display_name: CMake
      ssh_target: ${{ parameters.ssh_target }}
