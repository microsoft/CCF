start_node
dispatch_all
emit_signature,2

assert_is_primary,0

#Reconfiguration: add a new node
trust_node,2,1

emit_signature,2
periodic_all,10
dispatch_all
assert_is_backup,1

# Reconfiguration: add two nodes at once that are necessary to 
# commit reconfiguration entry itself
trust_nodes,2,2,3 # New configuration: [0, 1, 2, 3] in term 1
emit_signature,2
periodic_all,10
dispatch_one,0

state_all

dispatch_one,1

state_all # 1 isn't committed until one of the new nodes (either 2 or 3) acks
assert_commit_idx,0,2

dispatch_one,2 # initial NACK
periodic_one,0,10
dispatch_one,0 # Send a catchup
dispatch_one,2 # ACK the cactchup

state_all # 1 is now committed as new node 2 has acked
assert_commit_idx,0,6

periodic_all,10
dispatch_all
periodic_all,10
dispatch_all

state_all
assert_state_sync