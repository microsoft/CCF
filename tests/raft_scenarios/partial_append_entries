# Reproduce a trace-validation case where AppendEntries starts inside the
# follower's existing prefix, overlaps matching entries, and appends a suffix.
start_node,0
emit_signature,2
trust_nodes,2,1,2
emit_signature,2

# Node 2 is initially partitioned.
disconnect,0,2
disconnect,1,2
dispatch_all
periodic_all,10
dispatch_all

# Share one entry with node 1, then keep node 0's later entries private.
replicate,2,a
emit_signature,2
periodic_all,10
dispatch_one,0
dispatch_one,1
replicate,2,x
replicate,2,y
emit_signature,2

# Shift the partition so node 1 can win with node 2.
disconnect,0,1
connect,1,2
periodic_one,1,110
dispatch_all

replicate,3,ConfirmCommit
emit_signature,3
periodic_one,1,10
dispatch_one,1
dispatch_one,2
periodic_all,10
dispatch_one,1
dispatch_one,2
periodic_all,10
dispatch_one,1

# Reconnect node 0 while a queued AppendEntries from its old leadership term
# overlaps the shorter logs now held by nodes 1 and 2.
connect,0,1
connect,0,2
periodic_all,10
dispatch_all
periodic_all,10
dispatch_all

assert_state_sync
